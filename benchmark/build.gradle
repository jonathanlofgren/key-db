plugins {
    id 'java'
    id 'application'
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(16)
    }
}

mainClassName = 'benchmark.Runner'

repositories {
    mavenCentral()
}

test {
    useJUnitPlatform()
}